<?xml version="1.0" encoding="utf-8" ?>
<ListView xmlns="http://xamarin.com/schemas/2014/forms"
          xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
          x:Class="Xamarin.Presentation.Controls.LV.ExtendedListView"
          xmlns:crl="clr-namespace:Xamarin.Presentation.Controls;assembly=Xamarin.Presentation"
          x:Name="ExdListView"
          ItemsSource="{Binding ViewState.ViewCollection}"
          VerticalOptions="FillAndExpand"
          
          HasUnevenRows="true"
          >
    <ListView.Footer>
        <BoxView Style="{StaticResource SeparatorBox}" HeightRequest="10" />
    </ListView.Footer>
    <!--<ListView x:Name="ExdListView"
          ItemsSource="{Binding ViewState.ViewCollection}"
          VerticalOptions="FillAndExpand"
          CachingStrategy="RecycleElement"
          HasUnevenRows="true" />-->
</ListView>